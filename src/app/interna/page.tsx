// src/app/interna/page.tsx
'use client'; // Mantenha se você tiver interatividade cliente-side aqui

// Remova todas as importações de LucideIcons, useSession, useRouter, etc.
// Remova a lógica de collapsed, handleLogout.
// Remova a estrutura de div com a sidebar e main.

export default function InternaPage() {
  return (
    <div>
      <h1>Conteúdo da Página Interna</h1>
      <p>Este é o conteúdo específico da rota /interna.</p>
      {/* Coloque aqui o conteúdo real da sua página /interna */}
    </div>
  );
}